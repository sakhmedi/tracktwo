# Минимальные зависимости проекта.
# Чем меньше пакетов — тем выше шанс, что у судьи всё установится с первого раза.
opencv-python      # компьютерное зрение: чтение картинок, HSV, морфология, контуры, Hough
numpy              # массивы/матрицы — на них опирается OpenCV
